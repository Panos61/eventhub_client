mutation createEvent($options: EventInput!) {
  createEvent(options: $options) {
    errors {
      field
      message
    }
    event {
      title
      topic
      description
      adultsOnly
      time
      date
      extraInfo
    }
  }
}
